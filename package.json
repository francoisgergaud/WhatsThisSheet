{
  "name": "whats-this-sheet",
  "version": "1.0.0",
  "description": "A spreadsheet manager",
  "scripts": {
    "build": "tsc -w",
    "serve": "http-server ./ -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.7.2"
  }
}
